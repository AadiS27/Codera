version: '3.8'

services:
  # The api service will be added here later once we dockerize the Go app.
  # api:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - PORT=8080
  #   depends_on:
  #     - db
  #     - redis

  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: codera
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: code_executor
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

volumes:
  postgres_data:
